/** Canonical deep-link to an Intercom conversation in the Enterprise inbox. */
export function intercomUrl(conversationId: string) {
  return `https://app.intercom.com/a/inbox/teb21d17/inbox/conversation/${conversationId}?view=List`;
}
