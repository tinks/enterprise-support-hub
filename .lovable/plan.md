Rename the "Regenerate topics" button to "Generate new report" in `src/pages/Insights.tsx` (line 187). Also update the first-time label "Generate topics" → "Generate new report" for consistency, unless you'd prefer to keep that one separate.